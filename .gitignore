# Claude Code: machine-local pieces only. Project skills, commands, agents, and
# third-party notices under .claude/ are tracked so cloud sessions get them too.
# There is deliberately no tracked .claude/settings.json (this repo is public).
.claude/settings.local.json
.claude/launch.json
.claude/worktrees/

# Local-only projects and artifacts, per CLAUDE.md repo layout — never pushed
clinicalnotes/
backups/
scratch/
